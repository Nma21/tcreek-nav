import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <div>
            <button className='btn'>Sign Up</button>
        </div>
      );
}
 
export default Button;